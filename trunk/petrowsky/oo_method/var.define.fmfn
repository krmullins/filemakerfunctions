/*
var.define( name ; value )
RETURNS:  (bool)

This custom function is a wrapper for the
assignment(creation) of global variables.
*/

If ( Evaluate( "Let ( $$" & name & " = " & Quote ( value ) & "; \"\" )" ) = "?" ; 0 ; 1 )